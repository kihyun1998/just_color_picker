/// The visual layout style of the color picker.
enum ColorPickerType {
  /// Circular hue ring with an SV panel inscribed inside.
  wheel,

  /// Horizontal hue bar with a square SV panel above it.
  bar,
}
