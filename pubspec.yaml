name: just_color_picker
description: A customizable HSV color picker with circular hue wheel, saturation-value panel, alpha slider, and HEX input. No external dependencies.
version: 0.2.0
homepage: https://github.com/kihyun1998/just_color_picker
repository: https://github.com/kihyun1998/just_color_picker
issue_tracker: https://github.com/kihyun1998/just_color_picker/issues

topics:
  - color-picker
  - hsv
  - ui

environment:
  sdk: ^3.9.2
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
